from setuptools import find_packages, setup

setup(
    name="backend",  # Replace with your desired package name
    packages=find_packages(),  # Automatically find packages (directories with __init__.py)
    install_requires=[],
)
